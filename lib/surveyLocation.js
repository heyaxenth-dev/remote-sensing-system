/**
 * Default survey coordinates used until GPS is wired through the app.
 * Keep capture + dashboard weather aligned with the same reference point.
 */
export const SURVEY_LATITUDE = 11.2886;
export const SURVEY_LONGITUDE = 122.034;
